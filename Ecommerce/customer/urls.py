
from django.urls import path
from . import views

urlpatterns = [
    # path('index/', index.views, name='index'),
    # path('product_details/', product_details.views, name='product_details'),
    # path('product_cart/', product_cart.views, name='product_cart'),
]